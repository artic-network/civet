name: civet3
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - biopython
  - iqtree>=2.1
  - minimap2>=2.17
  - python=3.9
  - snakemake-minimal
  - cov-ert::jclusterfunk
  - gofasta>=0.0.5
# variables:
#   TEMPDIR:
#   CIVET_DATADIR: 
#   CIVETMODE: 
# civet now requires conda version 4.9 or later